String setupMessage() {
  return "Setup will guide you through the installation of FIDE.\n\n"
      "It is recommended that you close all other applications before starting Setup. "
      "This will make it possible to update relevant system files without having to reboot your Device.\n\n"
      "Click Next to continue.";
}

String setupTitle() {
  return "Welcome to FIDE Setup";
}

String notificationPermission() {
  return "Notification permission is required to inform you about important IDE events such as background tasks, build status, errors, and update notifications.";
}

String storagePermission() {
  return "Storage permission is required to access, create, and manage project files, caches, and SDK-related resources within the IDE.";
}

String appInstallPermission() {
  return "App installation permission is required to download and install required tools, SDK components, and updates directly from the IDE.";
}

String sdkDescription() {
  return "The development tools must be installed for the IDE to work. Clicking the done button will open terminal to install the tools. To install. follow the instructions here.\n\nOr read the documentation.";
}

String wifiSuggestion() {
  return "You are currently using mobile cellular data. This task may use about 2-3 GB of data, so it is recommended to use wifi.";
}

String storageMessage() {
  return "Available storage is low. Minimum 7-8 GB free space is required to install the development tools.";
}
